---
import { getTranslations } from '../i18n/translations';

const lang = Astro.currentLocale as 'en' | 'de' || 'en';
const t = getTranslations(lang).comparison;
---

<section id="at-a-glance" class="py-24 relative">
  <div class="container mx-auto px-6">
    <div class="max-w-3xl mx-auto">
      <!-- Section Header -->
      <div class="text-center mb-16" data-reveal>
        <h2 class="text-4xl md:text-5xl font-bold tracking-[-0.02em] mb-4 text-slate-900">
          {t.title} <span class="text-accent">{t.titleHighlight}</span>
        </h2>
        <p class="text-slate-600 text-lg">
          {t.subtitle}
        </p>
      </div>

      <!-- Capability table -->
      <div class="overflow-x-auto" data-reveal>
        <table class="w-full text-left border-collapse rounded-2xl overflow-hidden border border-slate-200">
          <thead>
            <tr class="bg-slate-100">
              <th class="py-4 px-5 text-sm font-bold text-slate-900">{t.featureLabel}</th>
              <th class="py-4 px-5 text-sm font-bold text-slate-900">{t.valueLabel}</th>
            </tr>
          </thead>
          <tbody>
            {t.rows.map((row: { feature: string; value: string }, idx: number) => (
              <tr class={idx % 2 === 1 ? 'bg-slate-50' : ''}>
                <th
                  scope="row"
                  class="py-4 px-5 align-top font-semibold text-slate-900 whitespace-nowrap border-t border-slate-200"
                >
                  {row.feature}
                </th>
                <td class="py-4 px-5 align-top text-slate-600 font-medium border-t border-slate-200">
                  {row.value}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>